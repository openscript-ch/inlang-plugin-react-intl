import { createPlugin } from '@inlang/core/plugin';

export const plugin = createPlugin(({env}) => ({
  id: 'openscript-ch.inlang-plugin-react-intl',
  config: async () => {
    return {

    }
  }
}))
